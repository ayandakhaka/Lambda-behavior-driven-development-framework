name: 🧪 LambdaTest BDD Automation CI

on:
  push:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      CI: true   # Detect CI mode in DriverManager
      MAVEN_OPTS: -Dmaven.test.failure.ignore=false
      BROWSERSTACK_USERNAME: ${{ secrets.BROWSERSTACK_USERNAME }}
      BROWSERSTACK_ACCESS_KEY: ${{ secrets.BROWSERSTACK_ACCESS_KEY }}
      BROWSERSTACK_BUILD_NAME: ${{ secrets.BROWSERSTACK_BUILD_NAME }}
      BROWSERSTACK_SESSION_NAME: ${{ secrets.BROWSERSTACK_SESSION_NAME }}
      BROWSERSTACK_BROWSER_NAME: ${{ secrets.BROWSERSTACK_BROWSER_NAME }}
      BROWSERSTACK_BROWSER_VERSION: ${{ secrets.BROWSERSTACK_BROWSER_VERSION }}
      BROWSERSTACK_OS: ${{ secrets.BROWSERSTACK_OS }}
      BROWSERSTACK_OS_VERSION: ${{ secrets.BROWSERSTACK_OS_VERSION }}

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: ☕ Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: 🧰 Install Chrome for headless testing
        run: |
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable
          google-chrome --version

      - name: 🏗️ Build and Run Tests (Headless Chrome)
        run: |
          mvn clean test -Dbrowser=chrome-headless

      - name: 📊 Upload Extent Report Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: extent-report
          path: |
            target/extent-report.html
            target/reports/**
            target/cucumber-reports/**
          retention-days: 7
